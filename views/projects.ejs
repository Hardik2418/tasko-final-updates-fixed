<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects - Tasko</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/responsive.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-container">
            <div class="navbar-logo">
                <a href="/">📋 Tasko</a>
            </div>
            <ul class="navbar-menu">
                <li><a href="/dashboard">Dashboard</a></li>
                <% if (user.role === 'admin') { %>
                    <li><a href="/admin-panel">Admin Panel</a></li>
                <% } %>
                <li><a href="/projects">Projects</a></li>
                <li><a href="/my-tasks">My Tasks</a></li>
                <% if (user.role === 'admin') { %>
                    <li>
                        <span class="notification-icon" onclick="toggleNotifications()">
                            🔔
                            <span id="unreadCount" class="unread-badge" style="display: none;">0</span>
                        </span>
                        <div id="notificationDropdown" class="notification-dropdown" style="display: none;">
                            <div class="notification-header">
                                <h3>Notifications</h3>
                                <button onclick="markAllAsRead()" class="btn btn-small">Mark All Read</button>
                            </div>
                            <div id="notificationList" class="notification-list"></div>
                        </div>
                    </li>
                <% } %>
                <li><span class="navbar-user"><%= user.name %> <span style="color: #888;">(<%= user.role === 'admin' ? 'Admin' : 'Employee' %>)</span></span></li>
                <li><a href="/api/auth/logout" class="logout-btn">Logout</a></li>
            </ul>
        </div>
    </nav>

    <main class="main-content">
        <div class="projects-container">
            <div class="projects-header">
                <h1>Projects</h1>
                <% if (user.role === 'admin') { %>
                    <button class="btn btn-primary" onclick="showCreateProjectForm()">+ New Project</button>
                <% } %>
            </div>

            <% if (projects.length > 0) { %>
                <div class="projects-grid">
                    <% projects.forEach(project => { %>
                        <div class="project-card">
                            <div class="project-card-header">
                                <h3><%= project.name %></h3>
                                <span class="role-badge"><%= project.creator._id.toString() === userId ? 'Admin' : 'Member' %></span>
                            </div>
                            <p class="project-description"><%= project.description %></p>
                            <div class="project-members">
                                <strong>Members (<%= project.members.length %>):</strong>
                                <div class="members-list">
                                    <% project.members.forEach((member, idx) => { %>
                                        <span class="member-tag"><%= member.name %></span>
                                    <% }); %>
                                </div>
                            </div>
                            <div class="project-actions">
                                <a href="/projects/<%= project._id %>" class="btn btn-small">View Details</a>
                                <% if (user.role === 'admin' || project.creator._id.toString() === userId) { %>
                                    <button class="btn btn-small btn-danger" onclick="deleteProject('<%= project._id %>')">Delete</button>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <p>No projects yet. <a href="#" onclick="showCreateProjectForm()">Create your first project</a></p>
                </div>
            <% } %>
        </div>

        <!-- Create Project Modal - ADMIN ONLY -->
        <% if (user.role === 'admin') { %>
        <div id="createProjectModal" class="modal" style="display: none;">
            <div class="modal-content">
                <span class="close" onclick="closeModal('createProjectModal')">&times;</span>
                <h2>Create New Project</h2>
                <form action="/api/projects" method="POST" class="form">
                    <div class="form-group">
                        <label for="projectName">Project Name</label>
                        <input type="text" id="projectName" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="projectDescription">Description</label>
                        <textarea id="projectDescription" name="description" rows="4"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">Create Project</button>
                </form>
            </div>
        </div>
        <% } %>
    </main>

    <footer class="footer">
        <p>&copy; 2026 Tasko. All rights reserved.</p>
    </footer>

    <script src="/js/main.js"></script>
    <script src="/js/projects.js"></script>
</body>
</html>
